--[[
  If the function we use at the npc is very common, then we can turn it into a utility function
  Essentially it will peek into a route. And if it sees some data, it will use that
  It will also have hard limit peeking of about like X seconds which we can pass through params

]]

local function peek() end

return peek
